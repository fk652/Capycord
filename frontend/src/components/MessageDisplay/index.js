import './MessageDisplay.css';

const MessageDisplay = () => {
  // get channelid
  // if channel not part of server, redirect back to /server/:id url
  // check channels redux state

  return (
    <div className="message-display">
    
    </div>
  )
}

export default MessageDisplay;